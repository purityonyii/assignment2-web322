<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= typeof title !== 'undefined' ? title : 'MealKit App' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS (meets rubric) -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

  <!-- your custom css (optional) -->
  <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-light">

  <header id="site-header" class="mb-4">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
        <a class="navbar-brand fw-bold" href="/">🍴 My Meal Kit Website</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div id="mainNav" class="collapse navbar-collapse">
          <ul class="navbar-nav me-auto">
            <li class="nav-item"><a class="nav-link" href="/mealkits">Meal Kits</a></li>
            <% if (user && user.role === 'clerk') { %>
              <li class="nav-item"><a class="nav-link" href="/mealkits/list">Clerk List</a></li>
            <% } %>
            <% if (user && user.role === 'customer') { %>
              <li class="nav-item"><a class="nav-link" href="/cart">Cart</a></li>
            <% } %>
          </ul>

          <ul class="navbar-nav ms-auto">
            <% if (!user) { %>
              <!-- support both /login and /log-in just in case -->
              <li class="nav-item"><a class="nav-link" href="/log-in">Login</a></li>
              <li class="nav-item"><a class="nav-link" href="/sign-up">Sign Up</a></li>
            <% } else { %>
              <li class="nav-item">
                <a class="nav-link" href="<%= user.role === 'customer' ? '/cart' : '/mealkits/list' %>">
                  Hi, <strong><%= user.name %></strong>
                </a>
              </li>
              <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>
  </header>

  <main class="container">
    <%- body %>
  </main>

  <footer id="site-footer" class="border-top py-4 mt-5">
    <div class="container small text-muted">
      <p class="mb-0">© <%= new Date().getFullYear() %> — Academic Integrity: This work is my own. — Onyinyechi Rita Ngaokere</p>
    </div>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
